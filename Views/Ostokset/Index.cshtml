@model IEnumerable<ShopMVC.Models.Ostokset>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.kuvaus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.summa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.päivä)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.kuvaus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.summa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.päivä)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
